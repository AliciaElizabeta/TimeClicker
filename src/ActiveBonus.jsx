import './ActiveBonus.css'

function ActiveBonus({}) {
    return (
        <div className="activeBonus">
            <button className="bonus">X2</button>
            <button className="bonus">Sale!</button>
            <button className="bonus">?</button>
        </div>
    )
}

export default ActiveBonus
